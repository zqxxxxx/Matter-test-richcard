name: Secret Scan

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions: {}

jobs:
  secret-scan:
    uses: Mininglamp-OSS/.github/.github/workflows/reusable-secret-scan.yml@v1
    permissions:
      contents: read
