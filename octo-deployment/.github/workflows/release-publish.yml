name: Release Publish

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to publish (e.g. v1.4.0)"
        required: true
        type: string
      validate_run_id:
        description: "Required: successful CI run ID from the tagged commit. Find it in the Actions tab → 'CI' workflow run → copy the run ID from the URL (…/actions/runs/<ID>)."
        required: true
        type: string
      draft:
        description: "Keep as draft instead of publishing"
        required: false
        type: boolean
        default: false

permissions: {}

jobs:
  publish:
    uses: Mininglamp-OSS/.github/.github/workflows/reusable-release-publish.yml@v1
    with:
      tag: ${{ inputs.tag }}
      validate_run_id: ${{ inputs.validate_run_id }}
      draft: ${{ inputs.draft }}
      ci_workflow_name: "CI"
    permissions:
      contents: write
      actions: read
