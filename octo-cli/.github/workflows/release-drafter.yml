name: Release Drafter

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions: {}

jobs:
  draft:
    uses: Mininglamp-OSS/.github/.github/workflows/reusable-release-drafter.yml@v1
    permissions:
      contents: write
      pull-requests: read
